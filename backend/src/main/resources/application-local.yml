# IDEA uses this profile for local debugging. Start MySQL and Redis separately
# with scripts/start-local-datastores.sh; Spring reads test-only values below.
spring:
  config:
    import: optional:file:backend/.env.test[.properties]
